<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('common/menu') %>
        <h1 align="center">Employees Details</h1>
        <table align="center">
            <thead>
                <tr>
                    <th>Employee Id</th>
                    <th>Employee Name</th>
                    <th>Employee Age</th>
                    <th>Employee Photo</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <% for(let i= 0; i < data.length;i++)
                    { %>
                    <tr>
                        <td><%= data[i].id %> </td>
                        <td><%= data[i].name %> </td>
                        <td><%= data[i].age %> </td>
                        <td><img src= "../public/emp_uploads/<%= data[i].photo %>" width="100" height="100"/></td>
                        <td><a href="/download/<%= data[i].photo %>"><button class="btn btn-primary">Download</button></a></td>
                    </tr>

                    <% } %>
                </tr>
            </tbody>
        </table>
</body>
</html>